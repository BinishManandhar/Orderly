<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.binish.orderly">

    <!-- font:awesome -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomePage" />
        <activity android:name=".customers_list" />
        <activity android:name=".ProfileLayout" />
        <activity android:name=".UpdateCustomerActivity" />
        <activity android:name=".ProfileDetail" />
        <activity android:name=".RegisterCompanyActivity" />
        <activity android:name=".CompanyListViewAdapter" />
        <activity android:name=".UpdateCompanyActivity" />
        <activity android:name=".CompanyProfileDetail" />
        <activity android:name=".ServiceTypesAdapter" />
        <!-- <activity android:name=".test" /> -->
        <activity android:name=".order_place" />
        <activity
            android:name=".CompanyDashboard"
            android:label="@string/title_activity_company_dashboard"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CompanyNavigation"
            android:label="@string/title_activity_company_navigation"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CustomerNavigation"
            android:label="@string/title_activity_customer_navigation"
            android:theme="@style/AppTheme" />
        <activity android:name=".OrderListViewAdapter" />
        <activity android:name=".CountDownTimerView" />
        <activity android:name=".CustomerProfileDetail" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />

        <receiver android:name=".Receiver"></receiver>

        <activity android:name=".NotificationAlarmActivity"></activity>
    </application>

</manifest>